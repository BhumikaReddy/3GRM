import time
import datetime

ts = time.time()
st = datetime.datetime.fromtimestamp(ts).strftime('%Y-%m-%d %H:%M:S')
print(ts)
print(st)
print ("JASSU MANOGO BUSINESS")

x=int(input("Enter number of mangoes : "))
y=int(input("Enter cost per each mango : "))

print(x)
print(y)

# x=30
# y=5

# calculation logic for total tomatos cost #
# total cost = number of tomato box * price per each box
# commission per box is 10 so, deducting 10 from each box
total_bill = (x*(y-10));

print("Total bill for your mangoes purchased", total_bill)


time.sleep(10)




x = int(input("enter number of mangoes:"))
y = int(input("enter cost per each mango:"))
print(X)
print(Y)
z = int(input("total number of mangoes:"))
print(z)
total (x+Y)
print(x+y)
print ("KCR MANOGO BUSINESS")

ts = time.time()
st = datetime.datetime.fromtimestamp(ts).strftime('%Y-%m-%d %H:%M:%S')
print(st)

x=int(input("Enter number of mangoes : "))
y=int(input("Enter cost per each mango : "))

print(x)
print(y)

# x=30
# y=5

# calculation logic for total tomatos cost #
# total cost = number of tomato box * price per each box
# comission per box is 10 so, deducting 10 from each box
total_bill = (x*(y-10));

print("Total bill for your mangoes purchased", total_bill)
